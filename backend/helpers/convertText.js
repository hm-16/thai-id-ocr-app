function extractInformation(text){
    //to implement logic to extract relevant info from ocr text
    const jsonData = {
        data: text
    };
    return jsonData;
}

module.exports = extractInformation;